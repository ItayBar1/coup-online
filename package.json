{
  "name": "coup-online",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build": "npm ci --prefix server && npm ci --prefix coup-client && npm run build --prefix coup-client",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "prettier": "^3.5.3"
  }
}
